# Financial Reports Application

## Overview
The Financial Reports Application is a web-based platform designed to manage financial reports for clients. It provides an admin panel for managing clients, projects, and payments, as well as a client dashboard for viewing financial reports.

## Project Structure
```
financial-reports-app
├── css
│   └── style.css        # Contains styles for the application
├── js
│   └── script.js        # Contains JavaScript code for application logic
├── index.html           # Main HTML file for the application
└── README.md            # Documentation for the project
```

## Getting Started

### Prerequisites
- A modern web browser (Chrome, Firefox, Safari, etc.)
- Basic knowledge of HTML, CSS, and JavaScript

### Installation
1. Clone the repository or download the project files.
2. Open the `index.html` file in your web browser.

### Usage
- **Admin Login**: Use the credentials `admin@finreport.co` / `password123` to log in to the admin panel.
- **Client Dashboard**: Clients can access their financial reports via a unique token link generated by the admin.

### Features
- Admin panel for managing clients, projects, and payments.
- Client dashboard for viewing financial summaries and reports.
- Responsive design using Tailwind CSS.
- Print functionality for generating PDF reports.

### Contributing
Contributions are welcome! Please feel free to submit a pull request or open an issue for any suggestions or improvements.

### License
This project is licensed under the MIT License. See the LICENSE file for more details.

# FinReport Pro - Plataforma de Reportes Financieros

Este proyecto es una Single Page Application (SPA) para la gestión de clientes y la generación de reportes financieros. Permite a un administrador gestionar clientes y generar enlaces de acceso temporal para que los clientes vean el estado de sus proyectos y pagos.

---

## 📝 Actualizaciones y Changelog

Aquí se registrarán todos los cambios importantes, nuevas funcionalidades y correcciones.

### v1.0.1 - 31 de octubre de 2025
*   **Corrección (Bug Fix):** Se corrigió un error visual en el panel de administración donde las vistas de "Proyectos" y "Pagos" mostraban los datos en formato JSON crudo en lugar de tablas formateadas.

### v1.0.0 - 31 de octubre de 2025
*   **Refactorización Inicial:** Se separa el código monolítico en una estructura de archivos organizada (`index.html`, `css/style.css`, `js/script.js`).
*   **Funcionalidad Base:**
    *   Módulo de autenticación para administradores.
    *   Panel de administración con CRUD para Clientes.
    *   Generación de tokens de acceso temporal (simulados con Base64) para reportes de clientes.
    *   Dashboard de cliente para visualizar proyectos, pagos y un resumen financiero.
    *   Sistema de logging para auditoría de acciones importantes.
    *   Funcionalidad de impresión (Generar PDF) para el reporte del cliente.

---

## 🐞 Bugs Críticos Conocidos

Actualmente no hay bugs críticos conocidos.

*   _(Aquí se listarán los bugs urgentes que necesitan atención inmediata)_

---

## ✅ Tareas Pendientes (To-Do)

Lista de funcionalidades y mejoras planificadas para futuras versiones.

### Backend y Seguridad
- [ ] Implementar un backend real (Node.js, Python, etc.) para gestionar la base de datos de forma segura.
- [ ] Reemplazar la codificación Base64 por verdaderos JSON Web Tokens (JWT) con firma digital.
- [ ] Crear endpoints de API seguros para todas las operaciones CRUD.

### Funcionalidades del Administrador
- [ ] Desarrollar el CRUD completo para **Proyectos**.
- [ ] Desarrollar el CRUD completo para **Pagos**.
- [ ] Añadir filtros y búsqueda en las tablas de Clientes, Proyectos y Pagos.
- [ ] Implementar paginación para las listas largas (clientes, logs, etc.).

### Funcionalidades del Cliente
- [ ] Integrar una pasarela de pago real (Stripe, PayPal) para que los clientes puedan pagar saldos pendientes directamente desde su dashboard.
- [ ] Añadir una sección para descargar facturas en formato PDF.
- [ ] Mejorar la interfaz de usuario del reporte del cliente con gráficos interactivos (ej. Chart.js).

### Mejoras Generales
- [ ] Escribir pruebas unitarias para las funciones críticas (autenticación, cálculos financieros, etc.).
- [ ] Mejorar la validación de formularios tanto en el cliente como en el servidor.


# 🗄️ Esquema de Base de Datos

El proyecto utiliza **PostgreSQL** a través de **Supabase DB**. El esquema ha sido diseñado para centralizar la gestión de clientes, proyectos, pagos y el registro de actividad, facilitando el sistema de reportes financieros y el acceso mediante tokens temporales.

---

## 💾 Scripts de Creación (Supabase/PostgreSQL)

El siguiente script define todas las tablas necesarias, incluyendo claves primarias (`PK`), claves foráneas (`FK`) y las restricciones de unicidad (`UNIQUE`).

```sql
-- Creación de la base de datos para Plataforma de Reportes Financieros para Clientes

-- 1. Tabla: clients (clientes)
CREATE TABLE clients (
    client_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL, -- Email para envío de links temporales
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
    updated_at TIMESTAMP WITH TIME ZONE
);

-- 2. Tabla: projects (proyectos)
CREATE TABLE projects (
    project_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    client_id UUID REFERENCES clients(client_id) ON DELETE CASCADE, -- Relación con la tabla clients
    name VARCHAR(255) NOT NULL,
    description TEXT,
    total_amount NUMERIC(10, 2) NOT NULL,
    status TEXT NOT NULL DEFAULT 'activo', -- Criterio de aceptación: (activos/cerrados)
    start_date DATE,
    end_date DATE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
    updated_at TIMESTAMP WITH TIME ZONE
);

-- 3. Tabla: payments (pagos)
CREATE TABLE payments (
    payment_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(project_id) ON DELETE CASCADE, -- Relación con la tabla projects
    amount NUMERIC(10, 2) NOT NULL,
    payment_date DATE NOT NULL,
    details TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now())
);

-- 4. Tabla: access_tokens (tokens de acceso temporal)
-- Usada para la autenticación del cliente (link temporal de 24h).
CREATE TABLE access_tokens (
    token_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    client_id UUID REFERENCES clients(client_id) ON DELETE CASCADE,
    token_value TEXT UNIQUE NOT NULL, -- Clave única y no nula para el enlace
    expires_at TIMESTAMP WITH TIME ZONE NOT NULL, -- La expiración debe ser validada (24 horas)
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now())
);

-- 5. Tabla: logs (registro de actividad)
CREATE TABLE logs (
    log_id BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY, -- ID autoincremental
    user_id UUID, -- ID del administrador (referencia a la tabla auth.users de Supabase)
    action_type VARCHAR(100) NOT NULL, -- Ej: 'LOGIN', 'CREATE_CLIENT', 'GENERATE_LINK'
    description TEXT,
    timestamp TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now())
);

-- Índices para optimizar consultas:
CREATE INDEX idx_projects_client_id ON projects(client_id);
CREATE INDEX idx_payments_project_id ON payments(project_id);
CREATE INDEX idx_access_tokens_client_id ON access_tokens(client_id);
CREATE INDEX idx_logs_timestamp ON logs(timestamp);